<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.9.xsd"
        objectQuotingStrategy="QUOTE_ONLY_RESERVED_WORDS">

    <changeSet id="2025-05-13-13-user-role" author="stepanov">
        <createTable tableName="USER_ROLE">
            <column name="USER_ID" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="ROLE" type="SMALLINT">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Создание составного уникального ключа -->
        <addUniqueConstraint
                tableName="USER_ROLE"
                columnNames="USER_ID, ROLE"
                constraintName="UK_USER_ROLE"/>

        <!-- Связь с таблицей USERS с каскадным удалением -->
        <addForeignKeyConstraint
                baseTableName="USER_ROLE"
                baseColumnNames="USER_ID"
                referencedTableName="USERS"
                referencedColumnNames="ID"
                constraintName="FK_USER_ROLE"
                onDelete="CASCADE"/>

        <!-- Индекс для USER_ID (опционально, если будут частые запросы по USER_ID) -->
        <createIndex
                tableName="USER_ROLE"
                indexName="IX_USER_ROLE_USER_ID">
            <column name="USER_ID"/>
        </createIndex>
    </changeSet>
</databaseChangeLog>